# 镜牢节点页面切换防卡死设计

成功点击节点的“进入”按钮后，寻路函数最多等待 15 秒，直到战斗编队页面可以识别，或路线图的 `legend_assets` 消失。等待期间不获取和消费下一条缓存路线，避免把地图到编队页面之间的过渡帧误判成仍需寻路。

如果等待超时，函数把控制权交回镜牢主循环，由主循环继续识别当前页面。寻路最终恢复循环也会在编队页面出现或路线图已经消失时立即返回，确保编队、战斗、事件和商店页面可以由主循环处理，而不会永久困在寻路恢复逻辑中。
